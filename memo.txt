

2017-06-26

* 一ヶ月分をまとめて生成する方法?



2016-11-22

* interactive にマークを設定できる様にしたい。

  その為には先ず内容を PHP から生成できる様にしなければならない。
  現在の実装では awk で内容を加工して生成する様になっている。
  PHP から awk を呼び出すのは大変なので、予め加工した物を保存する様にしておきたい。

  chkarxiv.sh を改造して様々のコマンドを受ける様にした。
  変換するコマンドを作成した。変換結果は .chkarxiv/html/* にためて置く事にした。

  さて次にしなければならない事は何か。php に出力する事である。
  そして対話的にマークを設定する為にはボタンを配置する必要がある。
  →ボタンの配置に関しては JavaScript で実行すれば良い。
  
